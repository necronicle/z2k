#!/bin/sh
# УСТАРЕЛО. Канонический релизный скрипт — scripts/release.sh.
#
# Здесь лежал второй, параллельный релизный скрипт: bash, авто-бамп номера
# (last_v+1), БЕЗ гейта на зелёный CI и без проверки соответствия префикса
# типу релиза. То есть у проекта было две двери в прод, и вторая обходила
# защиту, которую первая ставит.
#
# Хуже, что на неё указывали: скилл release-flow учил запускать именно этот
# файл, а тест релизной оснастки его же и проверял. Обход CI-гейта был не
# аварийным люком, а задокументированным путём.
#
# Файл превращён в шим, а не удалён намеренно: «no such file» — это
# приглашение достать скрипт из истории или написать заново, а мышечная память
# у команды «sh ./release.sh» длиннее, чем память об этом решении. Шим уйдёт
# вместе с тестом на него после того, как один релиз пройдёт каноном.
#
# Что делать вместо:
#   sh scripts/release.sh <patch|reinstall> "<описание для пользователя>"
# Подробности, включая порядок двух фаз и правила отката — RELEASING.md.

printf 'release.sh УСТАРЕЛ и ничего не делает.\n\n' >&2
printf 'Этот скрипт выпускал релиз В ОБХОД гейта на зелёный CI.\n' >&2
printf 'Канонический путь:\n\n' >&2
printf '    sh scripts/release.sh <версия> <patch|reinstall> "<описание>"\n\n' >&2
printf 'Порядок релиза, правила patch/reinstall и откат — см. RELEASING.md\n' >&2
exit 1
